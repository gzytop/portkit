# portkit 项目约定

## Design Context

### Users

开发者，在启动服务时撞上 `EADDRINUSE` / `端口已被占用` 而被打断，于是打开这个工具。

关键特征：**使用时长极短**。用户不想「使用」这个软件，只想在几十秒内查清是谁占了端口、把它干掉、然后回到编码。任何需要学习、需要浏览、需要思考的界面都是负担。

典型任务序列：打开 → 定位端口（通常是 3000 / 5173 / 8080）→ 确认占用者是不是自己的进程 → 终止 → 确认已释放 → 关掉。

### Brand Personality

**精密、可信、克制。**

参照对象是专业运维面板与仪器读数界面，不是消费级 App。用户在做破坏性操作（杀进程），界面必须让人觉得"这个工具知道自己在干什么"，而不是"这个工具很好看"。

信任来自准确和诚实：不谎报成功、不隐藏风险、不把受保护的进程和普通进程混在一起显示。

### Aesthetic Direction

- **方向**：精密工具感。信息密度高，但层次分明。
- **主题**：亮色与暗色双主题，界面内可切换。
- **约束**：tkinter（Python 标准库），零第三方依赖。没有 CSS，没有毛玻璃，没有 GPU 合成。一切颜色/间距/排版靠 tkinter 原语实现。
- **反面参照**：
  - 毛玻璃、紫蓝渐变、暗色配霓虹发光 —— 这些是 AI 生成界面的指纹，不要
  - 把每一行数据包成一张卡片 —— 表格就该是表格
  - 全部按钮同样醒目 —— 破坏性操作必须比"刷新"更有分量
  - 用 emoji 当图标

### Design Principles

1. **颜色只编码状态，不做装饰。** 每一种着色都对应一个用户需要区分的语义（可终止 / 系统受保护 / 内核残留 / 开发端口）。看不出含义的颜色一律去掉。

2. **破坏性操作要有视觉分量。** 终止进程不可撤销。主操作用实心危险色并保持视觉重量，次级操作退为低对比，避免误触。

3. **让用户要找的东西自己跳出来。** 用户找的几乎总是常用开发端口。这类行应当无需筛选就能被一眼扫到；系统进程噪音默认收起。

4. **零依赖不是简陋的借口。** 拿不到 CSS，就用感知均匀的色板（OKLCH 推导）、明确的字号阶梯、有节奏的间距去做出质感。约束改变手段，不降低标准。

5. **动效仅用于传达状态。** tkinter 里动画成本高且容易掉帧。只在状态真实发生变化时给反馈（加载中、操作完成），不做入场动画和装饰性过渡。

### Accessibility

- 正文与背景对比度不低于 WCAG AA（4.5:1）；状态色在两套主题下分别校验。
- 状态不能只靠颜色区分 —— 同时用文字标签（"系统"/"残留"），保证色觉障碍用户可用。
- 全部操作可键盘完成：`F5` 刷新、`Delete` 终止、`Tab` 遍历、`Enter` 确认。
