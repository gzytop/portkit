# 更新日志

本文件记录每个版本**面向用户的实际变化**，Release 说明里的「本次更新」一节由它自动生成。

维护约定：

- 每个已发布版本必有一个 `## vX.Y.Z — YYYY-MM-DD` 小节，标题格式不能改动，
  `.github/scripts/render_release_notes.py` 靠它定位内容。
- 打 tag **之前**先把 `## 未发布` 改名为对应版本号并补上日期。
  漏改会让 Release 构建直接失败——这是故意的，宁可发版失败，
  也不要再发出一份看不出改了什么的说明。
- 写「用户能感知到的差别」和「为什么改」，不要罗列文件名或函数名。
  纯内部重构、CI 调整、文档订正不必出现在这里。

## 未发布

（下一个版本的条目写在这里）

## v1.3.0 — 2026-08-18

### 修复

- **暗色主题下看不出复选框有没有勾选。** 方框底色用了亮灰，而勾选标记的颜色
  跟随文字色（近白），两者对比度只有 2:1 —— 三个开关看起来完全一样，
  没法判断「隐藏系统进程」到底开着没有。现在对比度是 7.5:1，勾没勾一眼可辨。
  这类组合已纳入自动断言，不会再悄悄退化。

### 改进

界面此前的问题不在配色，而在于**没有骨架**：五条通栏色带纵向堆叠、等宽等权重，
看上去只是几排控件摞在一起。这一版重塑了视觉层次：

- **快速释放端口**收成一张描边卡片，让主路径成为边界清晰的一块操作区，
  并补上「输入端口号后回车」的提示——此前输入框没有任何说明，得先猜填什么。
- **表格更像专业面板**：表头压深自成一层（原先比数据行还弱）、行高加大、
  斑马纹拉开到能看清行边界、鼠标悬停高亮当前行，明确「点下去会作用在哪一行」。
- **「处置」列不再刷屏**。原先每一行都写着「可终止」，一整列相同文字占了
  近百像素却零信息量；现在只有「系统保护」「内核残留」这类动不了的行才标注。
- **移除「进程详情」和「复制」两个按钮**。它们是针对某一行的操作，未选中行时
  是两个灰按钮，界面一打开就像坏了。这类操作本就该从行上发起，已移进右键菜单，
  并在操作栏右侧写明入口与快捷键。
- **状态栏分两级**：主计数（显示多少条、可终止多少）用正文色加粗，
  「系统共 N 条 · 最后刷新」退为小字，不再是一句同色平铺的长句。
- **筛选栏分组**：三个「显示哪些行」的开关收进带标签的一组，读起来是一个整体。
- 禁用态按钮改用专门的低对比配色，不再复用分隔线颜色。

### 其他

- Release 说明改为逐版本生成，内容取自 `CHANGELOG.md`。
  此前是一份静态模板，v1.0.0 到 v1.2.1 的说明除文件校验值外逐字节相同，
  用户无法从 Release 页面看出升级带来了什么。现在 CHANGELOG 里缺对应小节
  会直接让发版失败，宁可中断也不再发出没有信息的说明。

## v1.2.1 — 2026-08-18

### 修复

- 打包成 exe 后，筛选栏最右侧的「自动刷新」文字会被挤掉，只剩一个光秃秃的勾选框。
  根因是 exe 在 DPI 感知下中文字体变宽，`pack` 会压缩最后放入的控件。
  现在控件按语义分栏：筛选条件留在筛选栏，「自动刷新」移到「刷新」按钮旁边
  （它修饰的本来就是刷新行为），主题切换移到状态栏。

## v1.2.0 — 2026-08-17

### 新增

- **暗色主题**，界面右下角一键切换。
- **「处置」列**，用文字直接写明每一行「可终止 / 系统保护 / 内核残留」。
  原先这个信息只靠底色区分，色觉障碍用户无法分辨。

### 改进

- 重做整体视觉与信息层次：色板改由 OKLCH 感知均匀空间推导，
  两套主题下界面上真实出现的每个前景/背景组合都经过 WCAG AA 对比度校验。
- 按钮按破坏性排出视觉轻重：终止（实心红）> 刷新（实心蓝）> 详情/复制（描边）。
  此前「刷新」和「终止」都是实心高饱和色、几乎等价，对不可撤销的操作过于危险。
- 常用开发端口所在行加淡色底并加粗，不必筛选就能一眼扫到。
- 高分屏下界面不再被系统拉伸而发虚。

## v1.1.0 — 2026-08-17

### 修复

- **POSIX（macOS / Linux）上杀掉进程后会误报「进程仍在运行」。**
  进程已终止但尚未被父进程回收时会变成僵尸，而 `os.kill(pid, 0)` 对僵尸进程
  依然返回成功，于是每次正常终止都会看到一个假的失败提示。
- **非 UTF-8 控制台下脚本因中文输出而崩溃。** 在英文版 Windows（cp1252 控制台）上
  运行 `build_exe.bat` 必然以 `UnicodeEncodeError` 失败。

### 改进

- 加入 Windows / macOS / Ubuntu（3.12 与 3.9）四平台自动化测试，
  发版前强制全部通过才允许构建，避免只在开发机上验证过的代码流出。

## v1.0.0 — 2026-08-16

首个版本。

### 新增

- 查清是谁占着端口并安全释放它，命令行与图形界面同源。
- **快速释放端口** — 输入端口号回车，直接终止占用它的进程。
- **搜索与筛选** — 按端口号 / PID / 进程名过滤，可只看常用开发端口，默认隐藏系统进程噪音。
- **多选批量终止**、**进程详情**（看完整命令行，判断「这个 node 是我哪个项目」）、
  **自动刷新**（2 / 5 / 10 / 30 秒，适合等端口释放）。
- **安全边界**：`System` / `lsass.exe` / `svchost.exe` 等系统关键进程默认拒绝终止；
  先请求优雅退出、2 秒内没退才强杀；终止后复查端口是否真的释放，不给假结论。
- **免安装 exe**，下载双击即用，不需要装 Python。
